const errorcodes = {
	error11000: 'That email is already in use.Try another.',
	errorPwdMismatch: 'Those passwords did not match. Try again.',
	errorLoginMismatch: 'Wrong password'
};
export default errorcodes;
